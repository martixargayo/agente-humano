# Dev fixtures (input-only)

Estos fixtures representan **entrada sintética** para el pipeline real de evaluación.

- schema: `feedback_demo_fixture.v1`
- no contienen outputs de core/trajectory/ui report
- están centrados en diálogo (turnos user/assistant)
- `trace_digest` es opcional y reducido
